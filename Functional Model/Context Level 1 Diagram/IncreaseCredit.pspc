increase_credit(orderer_id, amount) {
    orderer =  User.find(orderer_id);
    transacted_money = make_bank_transaction(amount);
    if(transacted_money == amount) {
        orderer.credit += amount;
        orderer.save();
        return True;
    } else {
        return False;
    }
}
